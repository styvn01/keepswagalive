import fetch from "node-fetch";
export async function handler(event){ try{ const {email,amount,itemId} = JSON.parse(event.body||'{}'); const initRes = await fetch('https://api.paystack.co/transaction/initialize',{method:'POST',headers:{Authorization:`Bearer ${process.env.PAYSTACK_SECRET_KEY||'sk_test_PAYSTACK'}`,'Content-Type':'application/json'},body: JSON.stringify({email,amount: Math.round(amount*100),callback_url: (process.env.SITE_URL||'https://example.com') + '/paystack-callback',metadata:{itemId}})}); const data = await initRes.json(); return {statusCode: initRes.ok?200:500, body: JSON.stringify(data)} }catch(e){ return {statusCode:500, body: JSON.stringify({error: e.message})}}}